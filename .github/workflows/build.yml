name: build

# 三平台构建：PyInstaller 不能交叉编译，所以 Windows/macOS/Linux 各在自己的
# runner 上构建单文件二进制。push tag（v*）时自动挂到 Release。

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

# 需要 contents: write 才能在 tag 时创建/更新 Release
permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # Linux binary targets Ubuntu 20.04 / glibc 2.31 so it can run on
        # common older distributions and the project's WSL test environment.
        os: [windows-latest, macos-latest, ubuntu-20.04]
        include:
          - os: windows-latest
            artifact: windows
          - os: macos-latest
            artifact: macos
          - os: ubuntu-20.04
            artifact: linux
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller pytest

      - name: Run tests
        run: python -m pytest tests/ -q

      - name: Build binary
        run: python -m PyInstaller atd.spec --noconfirm --clean

      # 三个平台产物同名（atd/atd.exe），上传 Release 前按平台重命名防止覆盖
      - name: Rename binary per platform
        shell: bash
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            mv dist/atd.exe "dist/atd-${{ matrix.artifact }}.exe"
          else
            mv dist/atd "dist/atd-${{ matrix.artifact }}"
          fi
          ls -la dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/atd-*

      - name: Attach to release (tag only)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/atd-*
          fail_on_unmatched_files: false
