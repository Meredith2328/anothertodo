# 更新日志

本文件记录用户能感觉到的变化。日期按发布日算。

## 0.2.1

这一版的主线是把之前审查里发现的问题逐个修掉，顺带把几个「存了但用不上」的字段补成完整功能。

### 彻底移除 Python 旧实现

仓库最早是 Python 实现，去年整体迁到 TypeScript / Node 之后，那份代码一直留在树里没人调用。这一版把它彻底删掉了：

- 删除 `atd/*.py`（15 个模块）、`atd_entry.py`、`atd.spec`、`pyproject.toml`、`requirements.txt`、三个 `tests/test_*.py` 和 `tools/screenshot.py`
- 删除 PyInstaller 的构建流水线 `.github/workflows/build.yml`；三平台单文件可执行早已由 `build-node.yml` 的 Node SEA 流程产出
- `bin/atd.mjs` 去掉 `ATD_ENGINE=python` 回退分支
- 两个 README 里所有暗示「还有另一个实现」的措辞都清掉了

**这不影响你的数据。** `~/.atd/tasks.jsonl` 还是同样的纯文本 JSONL，格式没变，老版本写下的任务照样能读。

`DESIGN.md` 也从当年 735 行的迁移计划改写成了现在的架构说明，顺带修掉旧文档里两处对同一个事件给出不同载荷的矛盾。

### 修掉的问题

- **带 `~wait` 的任务会从议程里凭空消失。** README 自己的例子 `atd add "修车 ~下周一"` 加完就再也看不见了：`add` 一律写成 `todo` 状态，而分组时「无日期」桶又把带 wait 的 todo 排除在外，两头落空。现在未来的 `~日期` 直接落成 waiting，wait 只决定「现在露不露面」而不决定归属，已过期的 wait 会重新出现。
- **`atd list -低` 会被当成未知命令行选项拒绝。** README 里的旗舰查询示例根本跑不通。
- **`+高` 按标签匹配，`-低` 按档位匹配，两边不对称。** 现在两侧都是「先查档位再当标签」。
- **`wait:` 的范围写法逻辑反了**，`wait:week` 反而筛出所有*没有*等待日期的任务。
- **`edit` 只能覆盖字段不能清空**，加错的日期永远删不掉。
- **`config set` 只认两段 key**，README 说紧急度系数可调，实际上 `priority.urgency.overdue` 设不了。
- **`done` 一个已完成的任务会静默刷新完成时间**，把真正做完的时刻冲掉。
- **`隐藏(等待未到) 0 项` 在没有隐藏项时也照样打印。**
- **过了时间的 `meeting` 不计入逾期**，会掉进分组的缝里。

### 一行输入新增三种语法

```bash
atd add "买礼物 明天 >>她说想要那个手账本，别买错型号"   # >> 之后到行尾整段是备注
atd add "倒垃圾 *每天 晚上8点"                          # 重复任务
atd edit a1b2 "-due -#临时"                             # 清空字段 / 摘掉一个标签
```

- **备注 `>>`**：之后到行尾整段作为备注，里面的 `#` `@` `proj:` 和日期都不再被解析成字段。单写一个 `>>` 表示清空。`notes` 字段以前只存在于 schema 里，一处都没用到。
- **重复规则 `*`**：`*每天` `*每2周` `*每周三` `*每月` `*每年` `*工作日`，英文写法 `*daily` `*2w` `*weekly:wed` `*weekdays` 也认。完成时会**另开一条新任务**（新 id），截止日期、等待日期和提醒时间整体往后平移，原任务留在历史里——这样「哪天真的做了」不会被下一次覆盖。按月推进时 31 号遇上短月会压到月末（1 月 31 日 → 2 月 28 日），不会滚到下个月。
- **清空字段**：`-due` `-proj` `-标签` `-优先级` `-等待` `-父` `-备注` `-重复` `-提醒`，`-#某标签` 只摘掉一个标签。中英文别名都支持。

### 查询语法扩充

- `wait:week` `wait:any` `wait:none` `wait:after:2026-09-01`；`due:none` `due:any` `due:month` `due:nextweek`
- `parent:<id前缀>` 找子任务，另有 `parent:none` / `parent:any`
- `has:notes` `has:recur` `has:due` `has:reminder` `has:parent` `has:tags` `has:time`，前面加 `-` 取反
- `-status:waiting` `-project:读书` `-#标签` `-/关键字` 这类取反过滤
- `!高` 与 `-高` 等价，`!` 不会跟 shell 的选项解析打架
- `/关键字` 现在也搜项目名和备注，不再只看标题和标签
- 查询里点名写了 `wait:` 条件时，等待未到期的任务不再被折叠隐藏

### 子任务真的会显示了

`^父任务id` 记的父子关系以前只是存着，界面上完全看不出来。现在列表和 TUI 里子任务会缩进排在父任务下面（支持多层）。完成父任务时会点名报出还没完成的子任务，`atd done <id> --with-subtasks` 才一起完成；删除父任务时会提示哪些子任务变成了孤儿。

### 新增命令

| 命令 | 作用 |
|---|---|
| `atd cancel <ids...>` | 取消任务（保留记录，不同于删除） |
| `atd meeting <ids...>` | 标记为会议 |
| `atd todo <ids...>` | 退回待办，并清掉等待日期 |
| `atd wait <ids...> --until 下周一` | 押后到指定日期 |
| `atd projects` / `atd tags` | 按项目、标签汇总未完成 / 已完成 / 逾期数 |
| `atd stats` | 整体状况：各状态数量、逾期、本周到期、重复、备注、子任务、待发提醒、近 7/30 天完成量、最紧急的五条 |
| `atd export [查询] -f json\|csv\|markdown -o 文件` | 导出，可带查询条件 |
| `atd sync --setup <url>` | 直接配置 origin 远程，不用自己敲 `git remote add` |
| `atd config get <key>` | 读单个配置项 |

`cancelled` 和 `meeting` 这两个状态以前压根没有入口。

其他改进：`atd --help` 现在每条命令都有说明，顶层帮助还附了一行输入语法、查询语法和示例；`atd show` 默认输出给人读的字段表（含备注、提醒投递状态、父子任务），`--json` 才给原始 JSON；`atd sync-status` 从一行概要变成分支、远程地址、未提交变更数、领先落后提交数和最近一次提交；`config set` 支持任意层级 key，拼错的 key 和类型不对的值会当场拒绝，不会写坏配置文件。

### TUI

- `l` 或 `→` 打开详情浮层，能看到备注、提醒投递状态和父子任务；浮层里 `j`/`k` 翻上下一条，`e` 直接编辑
- 空格打勾多选，`Ctrl+A` 全选本屏，有勾选时 `d x c w o s` 批量执行，顶栏显示 `◉N`
- `x` 删除会先弹确认框，只认 `y` / `Enter`，其他键一律当取消
- 新增 `c` 取消、`o` 重新打开、`s` 提醒推迟 10 分钟
- `PgUp` / `PgDn` 翻页（这两个键以前在类型里声明了却没接上）
- `Esc` 先清多选，再当退出的第一下
- 新增 `:cancel` `:meeting` `:todo` `:wait <日期>` `:snooze <分钟>` 命令

### 界面语言

配置新增 `[ui] lang`，可选 `auto`（默认，跟随 `ATD_LANG` / `LC_ALL` / `LANG`，认不出来按中文）、`zh`、`en`。切成 `en` 后议程分组名、日期列、重复规则描述和字段名表都是英文。一行输入语法和查询语法在两种语言下完全一样。

```bash
atd config set ui.lang en
```

目前覆盖列表和详情这类每次都会看到的文案；命令帮助、TUI 帮助面板和操作回显还是中文。

### 内部改动

这些不影响用法，但对后续开发有影响：

- 议程分组现在带稳定的 `key`，上色和逻辑判断都用它。以前 TUI 靠显示名查颜色、CLI 靠 `name.startsWith("隐藏")` 判断隐藏行，换个界面语言就全失效。
- 终端显示宽度的计算抽到 `src/core/width.ts`，CLI 表格和 TUI 共用一套。以前 CLI 用全角空格凑对齐，中文一多列就错位。
- 批量操作改成逐条报错继续，一条失败不再带走后面几条。
- 测试从 100 个增加到 155 个。

### 已知问题

`npm audit` 报了 6 个依赖漏洞，这一版没动，因为修复都要跨大版本升级，不适合塞进一个补丁版本：

- **nodemailer 6.x**（运行时依赖，2 个 high）：SMTP 命令注入和收件域名解析歧义。只有配置并启用了 `email` 提醒 hook 才会走到这段代码，默认的 `toast` 通知不受影响。修复需要升到 nodemailer 9。
- **vitest / vite / esbuild**（仅开发依赖，1 个 critical + 3 个 moderate）：都是开发服务器相关的任意文件读取。这几个包不进发行产物，本项目也不跑 vite dev server 或 vitest UI。修复需要升到 vitest 4。

两处升级计划放到下一个版本单独做，好单独验证。

---

## 0.2.0

TypeScript / Node 实现成为默认。三平台 Node SEA 单文件可执行（无需安装 Node），Ink TUI 支持鼠标点击与滚轮，一行输入支持英文日期、英文紧急度短语、12 小时制和多词 `~wait` 日期。

## 0.1.x

Python 实现时期。CLI、TUI、模糊输入、提醒 hook、Git 同步、undo、归档、PyInstaller 三平台打包。
